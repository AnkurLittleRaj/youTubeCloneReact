import React from 'react'

function ButtonFunc({name}) {
  return (
    <div>

    <button className="p-2 m-2 border border-s-gray-200 text-center text-black">{name}</button>
    </div>
  )
}

export default ButtonFunc